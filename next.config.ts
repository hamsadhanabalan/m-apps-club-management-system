import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Use Next.js default bundler (Webpack) for full compatibility with type checking.
  // Turbopack is still experimental and can cause type validation issues in some setups.
};

export default nextConfig;
